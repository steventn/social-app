import React from 'react';
import DisplaySchedule from "./DisplaySchedule";

const Schedule = () => {
    return (
        <div>
            < DisplaySchedule />
        </div>
    );
};

export default Schedule;